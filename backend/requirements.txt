Flask==3.0.0
Flask-CORS==4.0.0
requests==2.31.0
gunicorn==21.2.0

if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)